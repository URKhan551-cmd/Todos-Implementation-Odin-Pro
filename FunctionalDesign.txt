tTodos


   get an aceess to dom values of input what user  keep it in variables.



export default function createTodo(valuesData){
  return {
id: uniqueRandomUUId(),
title: titleVariable,
.....

  }
};

export default function editTodo(id, newValues){
retrieve todo according to id;
// then call the storage function with that modified todo to store in storaeg.
stogrageTodo(todo);
};

export default completeTodo(id){
  // here we wil change its ststus.
}

export default function deleteTodo(id){
  todos.filter(todo => todo.id !== id);
// modify todos in storage again after deletion.
  storageTodo(todos);
}

export default function cleanInput(){
all the fields should be clean for next inpuut data.
}
