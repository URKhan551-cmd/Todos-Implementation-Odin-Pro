

renderProjects()
export function renderTodos(todos){  // todos comes from storage.js and pass through todoManager.getTodos();
   
}
renderTodoDetails()

clearTodoForm() 
export function openTodoDialog(){  // take this func to index.js where button lies
  clearTodoForm(formData);
  todoDialog.showModal();   
}
export function closeTodoDialog(){
  clearTodoForm(formData);
  todoDialog.close();
}
openProjectDialog()
closeProjectDialog()
